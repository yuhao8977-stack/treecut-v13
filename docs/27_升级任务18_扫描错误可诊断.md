# 升级任务 18：扫描错误可诊断

以前扫描只返回错误数量，用户不知道哪个文件失败、为何失败。本任务让 `ScanResult` 同时保存错误文件绝对路径、异常类型和系统说明。

- 总错误数仍完整统计。
- 详细记录最多 50 条，防止坏盘或权限问题产生无限内存占用。
- 桌面扫描结果显示前 3 条错误示例，超过时显示剩余数量。
- 权限拒绝测试验证路径、`PermissionError` 类型和系统消息均可见。
- 主版本 118 项测试通过。
