# 升级任务 26：发布清单与许可证审计

## 已完成

- 自动生成 `release/dependencies.lock.json`：记录便携 Python 中实际存在的包、版本和许可证。
- 自动生成 `release/models.lock.json`：记录模型路径、来源、版本线索、体积和许可复核状态。
- 自动生成 `release/THIRD_PARTY_NOTICES.md`：集中展示第三方组件和发布阻断项。
- 自动生成全部不可变文件的 SHA-256 清单，并提供独立校验器。
- 哈希过程支持 G 盘断点续算；数据库、日志、缓存等运行数据不纳入防篡改清单。
- 补充 `pyproject.toml` 的构建后端和三个正式命令入口。

## 审计发现与处理

- YOLO/Ultralytics 存在 AGPL 或商业授权要求：正式链路已迁移到 MIT 许可的 Florence 目标检测。
- `kaldiio` 随包许可证限制分发：随重复的 SenseVoice 运行链路一起从候选安装版移出。
- 候选版扫描结果：151 个 Python 分发包，未知许可证 0，自动判定的发布硬阻断 0。
- Chinese-CLIP 模型卡未明确声明权重许可证，仍保留人工复核标记；正式对外销售前应取得或保存上游书面许可依据。

## 验证

- 防篡改与可变运行数据边界测试通过。
- 候选版首次完整清单覆盖 34,618 个文件、12.8 GiB；该结果进一步触发任务 27 的运行时瘦身。

