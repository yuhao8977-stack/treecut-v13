# TreeCut v13 阶段验收记录

## 2026-08-03：新架构地基

- 当前电脑：8 线程 CPU、约 16GB 内存、无 CUDA；
- 自动模型方案：Florence-2-base + SenseVoice + YOLOv8n；
- 模拟 NVIDIA 8GB 环境：自动选择 Qwen3-VL + Whisper；
- 配置、日志、缓存、数据库、输出均位于 G 盘；
- 基础入口统一为同一个启动流程。

## 2026-08-03：素材发现和增量目录

- 能识别本机磁盘及其稳定卷编号，不只依赖 G/H 等盘符；
- 已通过“硬盘盘符发生变化后自动重连”模拟测试；
- 首次实扫 3 个真实视频，耗时约 0.266 秒；
- 第二次扫描识别为 3 个未变化文件，耗时约 0.035 秒；
- 数据库完整性检查结果为 `ok`；
- 已覆盖新增、修改、离线、重复内容和失败重试；
- 文件名分类明确标记为低置信度预分类，不冒充模型分析。

## 2026-08-03：现有模型迁入 v13

- Florence、Qwen3-VL、SenseVoice 和 YOLO 共 56 个文件已迁入；
- 逻辑大小约 6.94GB，使用同盘硬链接避免重复占用；
- v13 不再依赖 v12 的模型路径；
- 当前电脑自动选择 Florence + SenseVoice + YOLO。

## 2026-08-03：统一模型中心与离线配音

- 建立唯一模型状态表：模型用途、是否真实可用、降级方案和原因均可查询；
- “只有代码或空目录”不再被判断为模型可用；
- 安装 `sherpa-onnx 1.13.4` 及中英双语 VITS 离线模型；
- 官方模型压缩包为 167,006,755 字节，下载合并后通过压缩包检查；
- 已真实合成中文 WAV：44,100Hz、单声道、3.961 秒、349,432 字节；
- 当前自动状态已把本地 TTS 标为可用，不依赖网络；
- Faster-Whisper 运行组件已安装，但官方权重下载受当前网络 TLS 故障影响，仍诚实标记为未就绪；
- Whisper 未就绪时，软件继续使用已经可用的 SenseVoice，不阻塞 CPU 电脑。

## 2026-08-03：正式媒体检测链路起步

- FFmpeg/FFprobe 已迁入 v13 独立工具目录，使用硬链接避免重复占用约 198MB；
- 不再用“命令执行过”代表成功：时长、分辨率和视频轨道必须是有效值；
- 3 个真实视频全部通过读取，时长分别约 31.60、126.04、2.95 秒；
- 检出分辨率 576×1280、432×960、1080×1440；
- 正确识别其中 2 个带 AAC 音轨，1 个没有音轨；
- 这一步为后续抽帧、语音识别和画面模型分析提供可信输入。

## 当前自动测试

- 22 项测试全部通过；
- 另有 1 项真实离线中文语音生成测试通过；
- 所有本阶段新增文件、模型、缓存和输出均位于 G 盘。

## 2026-08-03：真实抽帧与视觉复核

- 正式分析工作器已经接通数据库任务队列；
- 每个视频真实抽取 10%、50%、90% 三个时间点，抽不到有效图片不能报成功；
- 3 个真实视频共生成 9 张有效 JPG，任务状态从 3 个待处理变为 3 个成功；
- Florence-2 在无显卡 CPU 上真实完成三张代表帧描述，总耗时约 186 秒；
- 视频 1 被识别为手机日历界面，视频 2 被识别为手机文字/数字界面；
- 上述两个录屏被标记为需人工复核、禁止自动选入成片；
- 视频 3 被识别为现代厨房，分类为 `interior_space`，允许进入后续匹配；
- 由此确认旧目录名“卖点展示”不可信，正式链路以后以真实画面结果为准。

## 下一阶段

- 完成 Chinese-CLIP 与 BGE-M3 本地权重；
- 建立正式视频分析工作器并处理 3 个真实素材任务；
- 继续双输出、Windows 界面和四层测试。

## 2026-08-03：29 个用户真实测试视频

- 桌面原始素材只读，已复制到 G 盘隔离测试目录；
- 共 29 个 MP4、2,843,650,130 字节，复制前后完全一致；
- 首次目录扫描：新增 29、错误 0、重复 0；
- 29 个视频参数读取全部成功，全部具有音轨，总时长约 1,955.88 秒；
- 共抽取 87 张有效测试帧，29 个分析任务全部成功，耗时约 88 秒；
- Florence 使用 4 个一批的 CPU 推理方式完成 29 个代表帧，后 25 个耗时约 764 秒；
- 业务分类与画面分类正式拆开，最终得到客户案例 13、产品展示 15、室内空间 1；
- 原始文件与 G 盘测试副本的清理边界记录在 `03_测试素材清理清单.md`。

## 2026-08-03：SenseVoice 分段时间轴首次验证

- 23.08 秒真实视频被正确切成约 20.02 秒和 3.07 秒两段；
- 模型识别到背景音乐，但只产生标点和单个字母，不构成有效口播；
- 已修复旧逻辑可能把 `. I.` 判定为有口播并生成垃圾字幕的问题；
- 当前时间轴明确标记为固定音频分段精度，不冒充 Whisper 的词级时间戳；
- 当前自动测试为 28 项，全部通过。

## 2026-08-03：四车道语音验证

- 同一次模型加载连续处理 4 个不同真实视频；
- 4/4 均识别出有效中文口播，分别生成 4、5、5、7 个时间段；
- 转写文字长度分别约 528、630、643、814 字；
- 正确区分另一条纯音乐视频为无口播，不生成垃圾字幕；
- 发现 SenseVoice 会把行业词“岛台”误识别为“倒台/导台”；
- 新增可追溯行业词纠正层，保存纠正来源、目标和次数；
- 当前自动测试为 29 项，全部通过。

## 2026-08-03：卖点匹配、编排与首条真实 MP4

- 建立可解释本地匹配器，不使用随机选材；
- 每个匹配结果记录分数、命中词、证据来源和匹配方法；
- 被标记为敏感或需复核的两个手机录屏不会进入自动剪辑；
- 真实卖点测试从 32 个候选中得到 30 个可用候选、14 个相关匹配；
- 生成完整 30 秒决策表，使用 8 个不同视频，避免同一素材重复占位；
- 已把语音分段接入剪辑点定位，有转写的素材优先跳到命中卖点的时间段；
- 首条真实预览 MP4：30.033 秒、540×960、3,490,278 字节；
- 渲染耗时约 16.52 秒，FFprobe 回读验证时长和尺寸正确；
- 当前预览刻意不带声音，用于隔离验收选材与画面编排；
- 当前自动测试为 35 项，全部通过。

## 2026-08-03：离线配音、字幕与背景音乐

- 使用本地 Sherpa-ONNX 为 115 字中文文案生成离线配音；
- 配音实际时长约 20.64 秒，自动生成 5 条中文字幕；
- 首次封装暴露字幕轨导致视频从 30 秒截成 20.47 秒的问题，该版本不作为成功结果；
- 修复后以画面时长为唯一上限，配音结束后补静音，输出恢复为 30.033 秒；
- 加入 10% 音量的本地背景音乐，末尾 2 秒淡出，配音保持主音量；
- 最终预览同时包含 H.264 视频、AAC 音频和 mov_text 中文字幕三条轨道；
- 字幕包回读数量为 5，避免空字幕轨冒充成功；
- 当前有声音乐预览约 4,063,118 字节；
- 当前自动测试为 40 项，全部通过。

## 2026-08-03：烧录字幕与剪映草稿双输出

- 下载并随软件保存 Noto Sans CJK SC 开源中文字体，大小 16,437,364 字节；
- 同目录保存 SIL Open Font License 1.1 许可证文件；
- FFmpeg 已确认支持 libass 的 `ass` 与 `subtitles` 渲染器；
- 字幕实际烧录进画面，抽取第 2 秒验证帧确认中文、白字黑边和底部位置正常；
- 烧录版本保持 30.033 秒并保留配音/音乐音轨；
- 建立全新的 v13 剪映草稿适配器，没有迁移旧版重复调度器；
- 首份草稿包含 4 条轨道：主画面、配音、背景音乐、字幕；
- 草稿包含 8 个视频片段、1 个配音片段、1 个音乐片段和 5 个字幕片段；
- `draft_content.json`、`draft_meta_info.json`、`draft_settings` 均真实生成并回读；
- 草稿总时长字段为 30,000,000 微秒，结构校验通过；
- 当前自动测试为 43 项，全部通过。

## 2026-08-03：正式生产链路、Windows 界面与本地 API

- 建立统一 `ProductionService`，正式入口只经过匹配、编排、渲染、配音字幕、成片、剪映草稿这一条链路；
- 12 秒端到端验收同时产出可播放 MP4、非空剪映草稿和可追溯报告，总耗时约 27.38 秒；
- 最终 MP4 为 12.033 秒，包含 H.264 画面与 AAC 声音，大小 1,426,327 字节；
- 建立中文 Windows 桌面界面，可扫描素材、填写卖点和配音文案、选择时长及两种输出；
- 桌面窗口已在真实 Tk 8.6 环境启动，进程存活且响应正常；
- 建立只允许本机访问的 FastAPI 接口；`0.0.0.0` 等对外监听地址会被拒绝；
- 真实启动接口后，`/health` 返回版本 `13.0.0.dev1`，数据、模型、缓存和临时目录全部位于 G 盘；
- `/catalog` 实际返回 2 个在线素材源、32 个视频、32 个成功分析任务；
- Python UTF-8 回读确认“学友”“理解视频画面”和中文素材路径均未损坏；
- 增加 Pydantic v1/v2 请求解析兼容，便于以后在不同电脑的便携运行环境中使用；
- 当前自动测试为 47 项，全部通过；本次 SSD 重插后发现临时借用的旧运行环境未携带 pytest，将在独立便携运行环境阶段消除该依赖混用问题。

## 2026-08-03：G 盘独立便携运行环境

- 将 Python 3.12.13 标准库、推理依赖和 Windows DLL 合并到 `G:\TreeCut_v13\runtime`；
- 新解释器的 executable、prefix、base_prefix 均为 G 盘路径，不再依赖 C 盘 Codex Python；
- 首次严格导入发现 Torch `shm.dll` 缺少外部依赖，补齐旧环境 `Library\bin` 并建立自动 DLL 搜索配置；
- 再次视觉推理发现基础环境 NumPy 2.3.5 与旧推理环境 NumPy 1.26.4 混合残留；
- 将 v13 `site-packages` 精确同步为已验证推理环境，清除重复包，最终只保留 NumPy 1.26.4；
- Torch 2.3.1+cpu、Transformers 4.57.6、Scipy 1.13.1、Sklearn 1.9.0、FastAPI 0.141.1、Sherpa-ONNX 1.13.4 全部真实导入；
- v13 自带 Python 运行 47 项 unittest，全部通过，用时约 2.39 秒；
- 新环境真实生成 44.1 kHz、单声道、2.74 秒离线中文配音；
- Florence CPU 加载约 31.67 秒、单帧推理约 37.05 秒，正确识别打开门的黑色嵌入式烤箱及其控制面板和内胆；
- 桌面界面由 `G:\TreeCut_v13\runtime\pythonw.exe` 正式启动；
- 本地 API 由 `G:\TreeCut_v13\runtime\python.exe` 正式启动并返回正确健康状态；
- 启动脚本强制 Hugging Face、Torch、Python、Matplotlib、Ultralytics 和 pip 缓存全部写入 G 盘；
- pytest 网络安装因 TLS 中断失败，未采用关闭证书校验的危险绕过；项目改用 Python 自带 unittest 完成同一批 47 项测试。

## 2026-08-03：质量中心第一层真实输出门

- 确认 v13 此前只有步骤级异常检查，没有独立质量中心，旧版所谓自动质量分未迁入；
- 新质量中心不生成模拟总分，只保存每一项检查的实际值、预期值、是否通过和是否关键；
- 正式生产链路完成后必须用 FFprobe 回读成片，检查文件大小、实际时长、分辨率和音轨；
- 必须解析剪映 `draft_content.json`，检查三个核心文件、时间轴、轨道和视频素材；
- 质量报告写入 `production_report.json`，关键检查失败时不再显示“全部输出完成”；
- 对既有 12 秒真实项目回读：MP4 12.033 秒、540×960、1,426,327 字节且有音轨；
- 同项目剪映草稿为 12,000,000 微秒、4 条轨道、4 个视频素材，全部 10 项证据检查通过；
- 新增空草稿不得通过等回归测试，自动测试总数增至 49 项，全部通过。

## 2026-08-03：动态成片检查与可验证反馈学习

- 完整解码成片并检测整段黑屏、长时间冻结、静音比例、平均音量和峰值音量；
- 使用字幕第一条时间轴中点，比较烧录前后同一帧的顶部与底部真实像素差，避免“有 SRT 文件就算字幕成功”；
- 既有 12 秒成片黑屏、冻结和静音比例均为 0，平均音量 -33.0 dB、峰值 -16.7 dB；
- 既有成片字幕验证帧顶部差异 2.247、底部差异 9.987，字幕烧录证据通过；
- 新跑 8 秒正式双输出，共 4 个片段、16 个匹配候选，耗时约 30.24 秒；
- 新项目质量报告包含 17 项证据并全部通过：8.033 秒、540×960、有音轨、无黑屏/冻结/静音、字幕底部差异 21.255、草稿 4 条轨道和 4 个视频素材；
- 没有迁移旧版“自动学习、自我进化”的宣传性空架子，改为明确的保留、替换、永久禁用三种用户反馈；
- 相似需求下，保留反馈可追溯加分，替换反馈可追溯减分；永久禁用在所有需求下排除素材；
- 每次反馈保存原需求、动作、原因和时间，每次匹配保存基础分、调整值及反馈证据；
- 本地 API 新增反馈查询与写入端点，无效动作真实返回 422；
- Windows 结果窗口新增镜头列表和三个中文反馈按钮，小白用户无需调用 API；
- 修复 Windows 下 SQLite 事务结束但连接未立即关闭造成文件锁的问题；
- 自动测试增至 55 项，全部通过，桌面窗口实例化和事件循环验证通过。

## 2026-08-03：四车道真实队列与失败隔离

- 同时向本地 API 提交 4 个不同制作需求，覆盖双输出、仅 MP4、仅剪映三种模式；
- 当前 16 GB 无独显笔记本采用单工位排队，不允许四个 Torch/FFmpeg 同时争抢内存；
- 4 条车道全部依次经过 queued、running、success，4/4 成功、0 失败，总耗时约 60.86 秒；
- 四个项目分别得到 15、6、7、13 个匹配候选，每个计划使用 3 个真实片段；
- 双输出项目各 17 项检查、仅 MP4 项目 11 项、仅剪映项目 6 项，全部质量检查通过；
- 追加“成功→故意失败→成功”隔离测试：中间任务因 0 个合格素材明确失败，后一任务仍继续完成；
- 故意失败返回“合格素材不足：计划 0.0 秒，目标 5.0 秒”，没有冒充成功；
- 发现失败任务会因过早建目录留下空项目，已把项目目录创建延后到剪辑计划合格之后；
- 修复后再次提交无匹配请求，新增项目目录为 0，55 项回归测试仍全部通过。

## 2026-08-03：任务持久化与存储设备中断恢复

- API 任务的请求、排队、进度、结果和错误写入 G 盘 `production_jobs.db`，不再只保存在内存；
- 软件重启后，上次 queued/running 状态不会冒充继续运行，而是明确变成 failed/“上次运行被中断”；
- 中断任务完整保留原卖点、文案、时长和输出选择，可通过 retry 端点创建新的重试任务；
- 恢复演练中，模拟 running 任务启动后正确变为 failed，错误证据以 `Interrupted` 开头；
- 点击重试后新任务真实进入 queued 并成功生成 5 秒剪映草稿；
- 再次重启 API 后，任务历史仍保留 2 条：原任务 failed、重试任务 success，已完成任务未被重复标记；
- API 增加任务历史列表及启动时中断数量；
- Windows 桌面制作也写入独立 `desktop_jobs.db`，主界面新增“任务记录 / 重试”按钮；
- 桌面恢复模拟识别到 1 个旧任务，状态、中文提示和原卖点请求均正确保留；
- 素材盘离线模拟中，source id 保持不变、online 变为 0、媒体记录和原路径仍保存；
- 因此素材盘暂时拔除不会删除素材库知识；安装 v13 的 SSD 自身被拔掉时，需重新插回后再启动并重试任务；
- 新增两项任务日志回归测试，自动测试总数为 57 项，全部通过；
- 本轮临时 API 测试进程已全部关闭，不在后台长期占用资源。

## 2026-08-03：模型中心最终接线与正式分析工作器

- 模型盘点：Florence 0.434 GiB、LocalTTS 0.178 GiB、Qwen3-VL FP8 5.622 GiB、SenseVoice 0.879 GiB、Whisper 0 文件；
- 发现 Qwen、YOLO 此前只有策略名称或权重，正式分析工作器没有调用，属于空接线；
- Qwen 本地配置、两份权重、Qwen3VLProcessor 和 tokenizer 均能读取；新增 CUDA 推理适配器和失败回退 Florence；
- Qwen 自动启用门槛从错误的 6 GB 显存提高到至少 10 GB；8 GB 显存使用 Florence，避免爆显存；
- 当前无 CUDA 笔记本不能做 Qwen 实际推理，必须在目标台式机完成最终 GPU 验收；
- Whisper 适配器已建立，但只有同时存在 `model.bin`、`config.json` 和运行库才算就绪；当前空文件夹不会冒充模型；
- YOLO 正式接入分析报告，真实检测 oven 0.6742，以及 person 0.9262、bowl、cake、dining table 和坐标；
- 正式分析工作器现执行：媒体读取→三帧抽取→视觉描述→YOLO→语音识别→风险筛选→业务分类；
- 首次三帧 CPU 全分析因视觉与语音模型内存峰值导致底层进程退出，并遗留 running 任务；
- 新增遗留 running→retry 恢复，CPU 改逐帧并在语音前释放 Florence；同一任务重跑 1/1 成功，耗时约 210 秒；
- 进一步将 16 GB CPU 模式改为单代表帧，三帧文件仍保留供核查；第二条真实视频工作器本身 1/1 成功，耗时约 75.78 秒；
- 扫描按钮已接正式分析队列，会逐条显示第 N 个素材的分析进度，不再只登记 pending 任务；
- Ultralytics 曾因隔离脚本未应用统一环境在 C 盘创建 settings.json；已修复所有缓存子目录预创建，并精确删除 C 盘及 G 盘根部误建配置；
- 当前 Ultralytics 设置只位于 G 盘 `runtime_data/cache/ultralytics`；
- Chinese-CLIP 与 BGE-M3 明确降为未来可选增强包，v13 核心匹配使用已验证的画面描述、语音、分类、关键词和明确用户反馈；
- 模型最终去留详见 `04_模型最终去留_小白版.md`；
- 自动测试总数增至 61 项，全部通过。
