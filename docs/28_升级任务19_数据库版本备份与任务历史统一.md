# 升级任务 19：数据库版本、迁移备份与任务历史统一

## 修复内容

- 素材库 schema 明确为版本 3，任务库明确为版本 1，不再只靠运行时猜字段并直接 `ALTER TABLE`。
- 旧数据库迁移前使用 SQLite 在线备份 API 生成同盘一致性副本，备份位于 `runtime_data\database\backups`。
- 桌面和本地 API 统一使用 `jobs.db`，不再分别写 `desktop_jobs.db` 与 `production_jobs.db`。
- 初次启动会只读合并两个旧任务库；相同任务编号保留 `updated_at` 更新的记录；旧库不删除。
- 任务库启用 WAL 与 NORMAL synchronous，桌面/API并发写入使用 30 秒锁等待。
- 修复新迁移代码中 SQLite context manager 不自动关闭文件的 Windows 文件锁问题。

## 主库真实迁移证据

- 原桌面任务 1 条、API 任务 2 条。
- 新统一 `jobs.db` 共 3 条，全部保留。
- `materials.db` 从版本 0 升级到版本 3。
- 迁移前备份：`materials_schema0_to_3_1785777001751104900.db`，159,744 字节。
- 20 个并发任务写入测试无丢失，WAL 模式确认生效。
- 自动测试：121 项通过（同步候选版后复验）。
