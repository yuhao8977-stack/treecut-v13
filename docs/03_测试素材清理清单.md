# 测试素材清理清单

## 用户原始文件（禁止修改）

- 位置：`C:\Users\33186\Desktop\树剪调用`
- 数量：29 个 MP4
- 总大小：2,843,650,130 字节
- 处理原则：只读，不删除、不改名、不覆盖。

## TreeCut v13 测试副本（最终验收后可清理）

- 位置：`G:\TreeCut_v13\runtime_data\test_materials\desktop_树剪调用_20260803`
- 数量：29 个 MP4
- 总大小：2,843,650,130 字节
- 复制校验：逐文件相对路径、文件大小、总文件数和总字节数一致。
- 数据库类型：`test_folder`
- 清理条件：整个软件测试优化结束，并再次向用户确认准确目标后清理。
- 关联缓存：`G:\TreeCut_v13\runtime_data\cache\analysis_frames\4` 至 `32`，以及对应音频分段目录。

绝不把桌面原文件列入自动清理范围。

## v13 自动生成的测试项目（最终验收后可清理）

- 根目录：`G:\TreeCut_v13\runtime_data\output\projects`
- 包含便携环境、质量中心、四车道和故障隔离验收生成的 MP4、草稿、中间文件与报告；
- 这些项目当前仍用于后续回归核对，不提前删除；
- 最终清理前需再次枚举准确目录，并保留用户希望查看的示例成片；
- 不得把正式用户项目仅因位于该根目录就自动删除，最终版本需要为测试项目写入明确标记后才能自动清理。

## 模型与分析验证候选清理项

- `G:\TreeCut_v13\runtime_data\validation_full_worker`：正式分析工作器隔离数据库、帧和语音缓存；
- `G:\TreeCut_v13\models\Whisper-small`：当前 0 文件，只在最终确认不随核心版提供 Whisper 后删除空目录；
- C 盘误建的 `C:\Users\33186\AppData\Roaming\Ultralytics` 已精确删除，不再占用 C 盘；
- G 盘根部误建的 `G:\TreeCut_v13\Ultralytics` 已精确删除；
- Qwen 权重不可清理，必须保留到目标台式机完成 GPU 验收。
