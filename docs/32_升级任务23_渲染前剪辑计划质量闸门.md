# 升级任务 23：渲染前剪辑计划质量闸门

正式渲染前新增 `inspect_edit_plan`，不合格计划立即失败并写入项目 `STATUS.json`，避免浪费配音和渲染时间。

检查项：

1. 计划明确标记完整。
2. 时间线从 0 连续覆盖目标时长。
3. 每个源片段起止区间有效。
4. 每个镜头至少 1 秒，源长度与时间线长度一致。
5. 同一素材编号不重复。
6. 同一内容指纹不重复。
7. 每个镜头都有实际匹配词，匹配分不低于 0.10。

计划检查结果会合并进最终 `production_report.json`，不只是一次性内部判断。自动测试 128 项通过。
