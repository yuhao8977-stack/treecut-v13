# 升级任务 27：模型链路合并与运行时瘦身

## 正式架构调整

- Florence-2 同时负责画面描述和目标检测，移除正式链路对 YOLO/Ultralytics 的依赖。
- Faster-Whisper 成为唯一正式语音时间轴引擎；不再把 SenseVoice 当作隐藏备用链路。
- Qwen3-VL 5.62 GiB 权重拆到 `G:\TreeCut_v13_gpu_extension`，不再混入当前 CPU 核心安装版。
- 模型中心和深度诊断均已改为展示实际选用的 Florence、Whisper、Chinese-CLIP、BGE-M3 和本地 TTS。

## 候选版瘦身

- 移出 YOLO、SenseVoice、FunASR、kaldiio、ModelScope 及对应元数据。
- 移出 `runtime/Lib` 中与 `site-packages` 重复的包目录和 pip 遗留目录。
- 移出 Python/Torch 开发头文件与 CMake 构建文件。
- 移出旧框架遗留且正式链路未使用的 Polars、SciPy、scikit-learn、Numba、llvmlite、Matplotlib、FAISS、librosa 等包。
- 所有移出内容均保存在 `G:\TreeCut_v13_quarantine`，未直接删除。
- Torch 实际仍依赖 `torchgen`；回归测试发现后已恢复，避免过度精简。

## 真实验证

- Florence 目标检测真实加载并在视频抽帧上识别出 `mobile phone`。
- BGE-M3、Chinese-CLIP、Florence 权重在瘦身后均真实加载成功。
- 深度安装诊断全部通过，确认 Python、模型、缓存、数据库、输出均位于 G 盘。
- 候选桌面进程启动并保持响应；本地 API `/health` 返回 HTTP 200。
- 自动测试：133 项通过，1 条来自第三方 TestClient 的弃用提示。

## 尚未冒充完成的部分

- GPU 扩展目前只有模型权重，当前 CPU Torch 不支持 CUDA；必须在有 NVIDIA 显卡的目标电脑上构建并实测 CUDA 运行时后才能标为可发布。
- 最终安装版完成瘦身后须重新生成一次完整哈希并复算验证。
