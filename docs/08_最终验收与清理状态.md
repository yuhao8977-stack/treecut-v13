# 最终验收与清理状态

## 干净便携版验收

- 位置：`G:\TreeCut_v13_release_candidate`
- 大小：约 10.64 GiB
- 文件数：约 38,004（首次诊断后）
- 深度安装诊断：24/24 通过
- 独立 Python：通过，`base_prefix` 指向候选版自身的 `runtime`
- 核心依赖：Torch、Transformers、OpenCV、Sherpa-ONNX、FastAPI、Ultralytics 均真实导入
- 核心文件：Florence、SenseVoice、YOLO、离线 TTS、FFmpeg、FFprobe 均存在
- 桌面界面：已从候选版自身成功创建并关闭
- C 盘隔离：程序、模型、缓存、临时文件、日志、数据库和输出均指向 G 盘
- 干净性：不含 `tests`、测试视频副本和历史输出项目

验收报告：`G:\TreeCut_v13_release_candidate\runtime_data\logs\installation_diagnostic.json`

## 自动与真实测试证据

- 源码回归测试：61 项全部通过
- 四车道任务：4/4 成功
- 故障隔离：成功 → 故意失败 → 成功，后续任务未被拖垮
- 真实媒体：桌面 29 个 MP4 的只读副本参与扫描、分析和生产验证
- 真模型：Florence、SenseVoice、YOLO 和离线 TTS 已实际运行
- 真输出：MP4、剪映草稿、音轨、字幕和质量报告均真实生成并回读

## 尚未删除的内容

以下内容仍保留，不会自行删除：

- 用户桌面原始视频：`C:\Users\33186\Desktop\树剪调用`
- G 盘测试副本：`G:\TreeCut_v13\runtime_data\test_materials\desktop_树剪调用_20260803`
- v13 测试输出和分析缓存
- v12 及历史版本

只有用户明确同意具体目标后，才会精确清理测试副本和测试输出。干净交付候选版不得删除。

