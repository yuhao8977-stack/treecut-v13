# 升级任务 22：字幕多点验证与质量门槛收紧

- 完整解析 SRT 时间轴，抽样首条、中间条、末条字幕。
- 每个抽样点分别读取烧录前后画面，要求底部像素差 >= 3 且至少为顶部差异 2 倍。
- 三个抽样点必须全部通过；不再以第一条字幕代表整部视频。
- 黑屏比例上限由 10% 收紧到 3%。
- 冻结比例上限由 70% 收紧到 35%。
- 静音比例上限由 80% 收紧到 50%。
- 将测试树中剩余 13 个默认系统临时目录改到 `G:\TreeCut_NativeTemp`；无盘符临时目录调用归零。
- 自动测试：126 项通过。

这些门槛仍属于技术质量底线，不冒充审美评分；语义相关性、镜头重复、节奏和字幕文字正确性会继续单独建设。
